describe('one', function() {
    it('should be equal to two', function() {
        expect(1).toBe(2);
    });

    it('should be greather than zero', function() {
        expect(1).toBeGreaterThan(0);
    })
});